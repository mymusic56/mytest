<?php
//Connectors and APIs
//php7 removes the mysql extension, leaving only the mysqli and pdo_mysql two option

$servername = "localhost";
//if the host value is unset or empty, the client libray will default to a Unix socket
//connection on localhost.
$username = "root";
$password = "";
$database = 'charge';
